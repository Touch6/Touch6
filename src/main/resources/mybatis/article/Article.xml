<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.touch6.business.mybatis.article.ArticleMybatisDao">
    <insert id="writeArticle" parameterType="com.touch6.business.entity.article.Article">
        insert into t_b_article(
        id,uid,author,title,summary,
        content,category,type,tag,read_amount,
        comment_amount,approval_amount,oppose_amount,create_time,update_time,
        audit_status
        )
        values(
        #{id},#{uid},#{author},#{title},#{summary},
        #{content},#{category},#{type},#{tag},#{readAmount},
        #{commentAmount},#{approvalAmount},#{opposeAmount},#{createTime},#{updateTime},
        #{auditStatus}
        )
    </insert>
    <select id="findById" resultType="com.touch6.business.entity.article.Article" parameterType="string">
        select a.* from t_b_article a where a.id=#{0}
    </select>
    <update id="updateArticle" parameterType="com.touch6.business.entity.article.Article">
        update t_b_article
        <set>
            title=#{title},
            summary=#{summary},
            content=#{content},
            category=#{category},
            type=#{type},
            tag=#{tag},
            read_amount=#{readAmount},
            comment_amount=#{commentAmount},
            approval_amount=#{approvalAmount},
            oppose_amount=#{opposeAmount},
            update_time=#{updateTime},
            audit_status=#{auditStatus}
        </set>
        where id=#{id}
    </update>
    <select id="articleList" resultType="com.touch6.business.entity.article.Article" parameterType="string">
        select art.*
        from t_b_article art
        where art.uid=#{uid}
    </select>

    <select id="findAll" resultType="com.touch6.business.entity.article.Article">
        select art.*
        from t_b_article art
    </select>
    <update id="increaseCommentAmount" parameterType="string">
        update t_b_article
        <set>
            comment_amount=comment_amount+1
        </set>
        where id=#{0}
    </update>
    <update id="increaseApprovalAmount" parameterType="string">
        update t_b_article
        <set>
            approval_amount=approval_amount+1
        </set>
        where id=#{0}
    </update>
    <update id="increaseOpposeAmount" parameterType="string">
        update t_b_article
        <set>
            oppose_amount=oppose_amount+1
        </set>
        where id=#{0}
    </update>
</mapper>